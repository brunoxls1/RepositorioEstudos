//Dados Primitivos = String, number, undefined, null, boolean, symbol.
const nome = "bruno " ; //string
const nome1 = 'bruno' ; //string
const nome2 = `bruno`; //string
const num1 = 10 ; // number
const num2 = 10.52; // number
let nomeAluno; // undefined = não aparece em local algum da memória 
let sobrenomeAluno = null;  // Nulo -> não aparece em local algum da memória 
const aprovado = true; // Boolean = somente Verdadeiro ou Falso (lógico)

console.log(typeof aprovado)